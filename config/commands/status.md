---
description: Show current model, provider status, and session statistics
agent: build
---

Show the current status of the session:

1. Current model in use
2. Models that have failed so far in this session
3. Available fallback models remaining
4. How many times models have been switched
5. Any rate limit or error count per provider

Check the current config and auth status: